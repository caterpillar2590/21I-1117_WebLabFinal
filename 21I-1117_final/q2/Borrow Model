{
    member:{
        type:mongoose.Schema.Types.ObjectId,
        ref:"User"
    },
    book:{
        type:mongoose.Schema.Types.ObjectId,
        ref:"Book"
    },
    borrowDate:Date,
    dueDate:Date,
    returnDate:Date,
    status:{
        type:String,
        enum:["borrowed","returned"]
    }
}
